spring:
  application:
    name: FinalProjectWebServices
  profiles:
    default: normal
    group:
      normal:
        - h2
  jpa:
    hibernate:
      ddl-auto: validate
    properties:
      hibernate:
        format_sql: true
    open-in-view: false
  flyway:
    enabled: true
    baseline-on-migrate: true

springdoc:
  swagger-ui:
    path: /swagger-ui.html

server:
  error:
    include-message: always
